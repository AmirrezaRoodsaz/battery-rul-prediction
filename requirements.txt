# Battery RUL Prediction — pinned dependencies (Python 3.11, CPU-only).
# Pinned for reproducibility: a clean clone + `pip install -r requirements.txt` reproduces the environment.

# --- Core scientific stack ---
numpy==2.1.3
pandas==2.2.3
scipy==1.14.1
scikit-learn==1.5.2

# --- Data IO ---
h5py==3.12.1          # read Severson .mat files (MATLAB v7.3 == HDF5)
pyarrow==18.0.0       # parquet for the tidy processed form
requests==2.32.3      # raw-data download script
tqdm==4.67.1          # download / loop progress bars

# --- Modeling ---
xgboost==2.1.3        # gradient-boosted trees (CPU)
joblib==1.4.2         # persist trained model artifacts

# --- Visualization ---
matplotlib==3.9.2
seaborn==0.13.2

# --- Notebooks ---
jupyter==1.1.1
ipykernel==6.29.5

# --- Quality / tooling ---
pytest==8.3.4
black==24.10.0
ruff==0.8.4

# --- Optional demo (Phase 4 stretch) ---
streamlit==1.40.2
